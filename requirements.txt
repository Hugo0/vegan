### Runtime dependencies (top-level only; pip resolves transitive deps).
### Dev/test tools live in requirements-dev.txt.

# Web framework + server
Flask==3.1.3
gunicorn==26.0.0

# Blog content rendering
Markdown==3.10.2
